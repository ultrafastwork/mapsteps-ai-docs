1. Footer Builder Issue

   - Condition: Footer Builder Enabled
   - Issue: Copyright Widget Theme Author Instant Preview does not works.
   - Step to reproduce: Try to set the Theme Author and URL
   - Current Behavior: When set / change the theme author, the output is back to default layout ( standard layout ) and when this occurred, the others widgets live preview are also does not works.

2. Footer Builder Issue

   - Condition: Footer Builder Enabled
   - Issue: HTML 1 and HTML 2 text / paragraph has margin-bottom 20px by default
   - Step to reproduce: Try to add HTML 1 and HTML 2 widget and set the content
   - Current Behavior: The output has the more space on the bottom of the text, so the text does not on the right position ( vertical center ).

3. Footer Builder Issue

   - Condition: Footer Builder Enabled
   - Issue: Social Icons widget does not have default margin / padding on it
   - Step to reproduce: Try to add 2 or 3 social media item
   - Current Behavior: When there is more than 1 social icon added, the icon list does not have the margin / padding in the output
